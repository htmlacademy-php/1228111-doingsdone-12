<?php

/**
 * @param Array массив $_GET
 *  function pathinfo, получаем адрес текущего сценария
 *  function http_build_query, переводит массив в строку запроса, чтобы массив стал строкой
 * @param var $url, объединяет название страницы и параметры
 */

/*$categories = $_GET;

$categories['id'] = "id";
$scriptname = pathinfo(__FILE__, PATHINFO_BASENAME);
$query = http_build_query($categories);
$url = "/" . $scriptname . "?" . $query;*/

/*$category_id = $_GET['id'];
$get = get_by_category($category_id);


$categories['id'] = "id";
$scriptname = pathinfo(__FILE__, PATHINFO_BASENAME);
$query = http_build_query($categories);
$url = "/" . $scriptname . "?" . $query;*/
